## Trot race simulator

To run the project:
1. Clone the project
2. run `npm install` from the backend folde
3. run `npm run dev`
5. run `npm install` from the frontend folder
6. run `npm run start` 
